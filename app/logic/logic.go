package main

import "fmt"

func main() {
	// var balance int = 1000
	// var withdraw int
	// fmt.Print("Withdraw amount: ")
	// if fmt.Scan(&withdraw); withdraw < 0 {
	// 	fmt.Println("The amount must be greater than 0")
	// } else if withdraw >= balance {
	// 	//the branch
	// 	fmt.Println("The amount must be  smaller than or equal to balance")
	// } else {
	// 	//else branch
	// 	balance -= withdraw
	// 	fmt.Println("balance:", balance)
	// }
	n := -4
	if n := 7; n > 0 {
		fmt.Println(n, "is postitive")
	} else {
		fmt.Println(n, "is not postitive")
	}
	fmt.Println("n =", n)
}
